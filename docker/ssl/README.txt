# SSL certificate placeholders
# Generate real certificates with:
# certbot certonly --standalone -d yourdomain.com
# Then copy fullchain.pem to cert.pem and privkey.pem to key.pem

This directory should contain:
- cert.pem
- key.pem
